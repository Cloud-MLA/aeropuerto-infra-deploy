# Copiar a .env en VM-INGESTA (no se commitea). Terraform ya lo genera solo en /opt/ingesta/.env
# si aplicas vía terraform apply — este archivo es solo referencia para el paso manual (RUNBOOK 1.11).

# IP privada de la VM-DB
DB_HOST=10.0.10.XX

# Bucket S3 del data lake
S3_BUCKET=mla-aeropuerto-lake
