# Copiar a .env en la VM-DB (no se commitea). Usar contraseñas largas.
MYSQL_ROOT_PASSWORD=pon-una-contrasena-larga
POSTGRES_PASSWORD=pon-otra-contrasena-larga
# MongoDB queda sin auth dentro de la subred privada (solo lo alcanza sg-vm-prod / sg-vm-ingesta).
